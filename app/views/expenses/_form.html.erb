<%= form_for(expense) do |f| %>
  <% if expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
      <% expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :user_id,class:'sr-only' %>
    <%= f.text_field :user_id, class:'form-control',placeholder:'User Id' %>
  </div>

  <div class="form-group">
    <%= f.label :name,class:'sr-only' %>
    <%= f.text_field :name, class:'form-control',placeholder:'Name' %>
  </div>

  <div class="form-group">
    <%= f.label :amount,class:'sr-only' %>
    <%= f.text_field :amount, class:'form-control',placeholder:'Amount' %>
  </div>

  <div class="form-group">
    <%= f.label :tax,class:'sr-only' %>
    <%= f.text_field :tax, class:'form-control',placeholder:'Tax' %>
  </div>

  <div class="form-group">
    <%= f.label :tip,class:'sr-only' %>
    <%= f.text_field :tip, class:'form-control',placeholder:'Tip' %>
  </div>

  <div class="form-group">
    <%= f.label :category,class:'sr-only' %>
    <%= f.text_field :category, class:'form-control',placeholder:'Category' %>
  </div>

  <div class="form-group">
    <%= f.label :description,class:'sr-only' %>
    <%= f.text_area :description, class:'form-control',placeholder:'Description' %>
  </div>

  <div class="actions form-group clearfix">
    <%= f.submit 'Add new Expense',class:'btn btn-success pull-right clearfix'%>
     <%= link_to 'Back', expenses_path,class:'btn btn-danger pull-right clearfix' %>
  </div>
<% end %>
